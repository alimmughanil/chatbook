export const QuizIndex = (props) => {
  return <div className="">Daftar Kuis untuk Course: {props?.course?.title}</div>
}

export const ModuleIndex = (props) => {
  return <div className="">Daftar Modul untuk Course: {props?.course?.title}</div>
}
